# Copy to .env and fill in after you deploy the contract.
# Network the app talks to: "testnet" or "mainnet".
VITE_NETWORK=testnet
# The address that deployed the stackstreak contract (your wallet address).
VITE_CONTRACT_ADDRESS=
# Contract name (leave as-is unless you renamed it).
VITE_CONTRACT_NAME=stackstreak
